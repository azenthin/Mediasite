#!/usr/bin/env ts-node
/**
 * Seed audio_features.db with tracks from MusicBrainz + AcousticBrainz
 * 
 * DISABLED: This script requires sqlite3 and sqlite packages which have been removed
 * to maintain Next.js compatibility. These are not needed for the API.
 * 
 * The enhanced_music.db is pre-populated and used via CLI queries instead.
 */

console.log('❌ seed-musicbrainz.ts is disabled - sqlite3 packages removed for Next.js compatibility');
process.exit(0);

// Placeholder for disabled code - all original code removed to avoid build errors